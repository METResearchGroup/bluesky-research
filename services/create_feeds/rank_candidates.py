def rank_latest_candidates() -> dict:
    """Ranks latest candidates per user.
    
    Returns a dict whose keys are user IDs and whose values are
    the list of posts for their feed.
    """
    pass
