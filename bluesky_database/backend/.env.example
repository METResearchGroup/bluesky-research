# Redis Configuration
# Copy this file to .env and set your Redis password
# Example: REDIS_PASSWORD=your_secure_password_here

# Redis password (optional - leave empty for no authentication)
REDIS_PASSWORD=

# Note: If you set a password, make sure to update redis.conf to enable authentication
# by uncommenting the line: requirepass ${REDIS_PASSWORD}
