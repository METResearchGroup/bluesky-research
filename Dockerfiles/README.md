# Dockerfiles

A series of Dockerfiles for each service. These are currently deprecated as they are a remnant of when we had intended to deploy to an AWS-only architecture rather than the eventual hybrid on-prem + AWS setup that we ended up going with.

Here for future development, as we leave open the possibility of deploying this to a cloud-first architecture.
